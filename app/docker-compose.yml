version: '3'
services:

  rasa_server:
    container_name: "rasa_server"
    build:
      context: backend
    user: root
    ports:
      - "5005:5005"
    expose:
      - 5005

  action_server:
    container_name: "action_server"
    build:
      context: actions
    ports:
      - "5055:5055"
    expose:
      - 5055

  web_server:
    container_name: "web_server"
    image: nginx
    depends_on:
      - "rasa_server"
      - "action_server"
    ports:
      - "80:80"
      - "443:443"
    restart: always
    volumes:
      - ./web/conf/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./web/conf/explorer-ai.chat.conf:/etc/nginx/conf.d/explorer-ai.chat.conf:ro
      - ./web/conf/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./web/html/index.html:/var/www/html/index.html:ro
      - ./web/html/favicon.ico:/var/www/html/favicon.ico:ro
      - /var/www/.well-known:/var/www/.well-known:rw
      - ./certbot/conf/options-ssl-nginx.conf:/etc/letsencrypt/options-ssl-nginx.conf:ro
      - ./certbot/conf/ssl-dhparams.pem:/etc/letsencrypt/ssl-dhparams.pem:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - ./web/start.sh:/start.sh
    command: /start.sh

  certbot:
    container_name: "certbot"
    image: certbot/certbot
    volumes:
      - /var/www/.well-known:/var/www/.well-known:rw
      - ./certbot/conf/options-ssl-nginx.conf:/etc/letsencrypt/options-ssl-nginx.conf:rw
      - ./certbot/conf/ssl-dhparams.pem:/etc/letsencrypt/ssl-dhparams.pem:rw
      - /etc/letsencrypt:/etc/letsencrypt:rw
      - ./certbot/entrypoint.sh:/entrypoint.sh
    entrypoint: /entrypoint.sh
